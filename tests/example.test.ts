// Teste placeholder para validar a configuração do Jest nesta etapa inicial.
// Testes reais (lógica de avaliações, componentes, navegação) chegam na
// etapa de "testes".
describe("configuração inicial do projeto", () => {
  it("ambiente de testes está funcionando", () => {
    expect(1 + 1).toBe(2);
  });
});
